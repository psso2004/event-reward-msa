services:
  gateway:
    build:
      context: ./gateway
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      - PORT=3001

  auth:
    build:
      context: ./auth
      dockerfile: Dockerfile
    ports:
      - "3002:3002"
    environment:
      - PORT=3002

  event:
    build:
      context: ./event
      dockerfile: Dockerfile
    ports:
      - "3003:3003"
    environment:
      - PORT=3003
